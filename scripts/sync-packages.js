"use strict";

// Native CluauppLibs headers live in include/clpp/libs (Sweep, Spark, Keep, …).
// Package dumps must not overwrite those aliases.

console.log("sync-packages: native headers and runtimes are the source of truth");

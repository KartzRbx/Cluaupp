"use strict";
/**
 * Emit Typed DataModel profile JSON + CL++ IntelliSense header.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateDatamodelHeader = generateDatamodelHeader;
exports.profileWithoutIndex = profileWithoutIndex;
exports.writeDatamodelArtifacts = writeDatamodelArtifacts;
const node_fs_1 = __importDefault(require("node:fs"));
const node_path_1 = __importDefault(require("node:path"));
const datamodel_js_1 = require("./datamodel.js");
function safeIdent(name) {
    const cleaned = name.replace(/[^A-Za-z0-9_]/g, "_");
    return /^[A-Za-z_]/.test(cleaned) ? cleaned : `_${cleaned}`;
}
function emitStruct(node, structName, depth) {
    const pad = "\t".repeat(depth);
    const childEntries = Object.values(node.children);
    if (!childEntries.length) {
        return `${pad}// ${node.path} : ${node.className}\n`;
    }
    const lines = [];
    lines.push(`${pad}struct ${structName} {`);
    for (const child of childEntries.sort((a, b) => a.name.localeCompare(b.name))) {
        const field = safeIdent(child.name);
        const nested = `${structName}_${field}`;
        if (Object.keys(child.children).length) {
            lines.push(emitStruct(child, nested, depth + 1).trimEnd());
            lines.push(`${pad}\t${nested} ${field}; // ${child.className}`);
        }
        else {
            lines.push(`${pad}\t${child.className} ${field};`);
        }
    }
    lines.push(`${pad}};`);
    return lines.join("\n") + "\n";
}
function generateDatamodelHeader(profile) {
    const body = emitStruct(profile.root, "Dm_DataModel", 0);
    return [
        "#pragma once",
        "// Generated by Cluaupp Typed DataModel — do not edit.",
        `// Project: ${profile.projectName} (${profile.projectJson})`,
        `// schemaVersion: ${profile.schemaVersion}`,
        "// IntelliSense / static check aid. Runtime remains Roblox FindFirstChild / WaitForChild.",
        "",
        body,
        "// Root mirror (optional binding for IntelliSense only):",
        "struct Dm_DataModel Dm;",
        "",
    ].join("\n");
}
function profileWithoutIndex(profile) {
    const { byPath, ...rest } = profile;
    return { ...rest, paths: Object.keys(byPath).filter((p) => p !== "DataModel").sort() };
}
function writeDatamodelArtifacts(projectRoot, opts) {
    const profile = (0, datamodel_js_1.buildDatamodelProfile)(projectRoot, opts?.projectJson);
    if (!profile) {
        return null;
    }
    const outDir = opts?.outDir || node_path_1.default.join(projectRoot, ".cluaupp");
    node_fs_1.default.mkdirSync(outDir, { recursive: true });
    const profilePath = node_path_1.default.join(outDir, "datamodel-profile.json");
    node_fs_1.default.writeFileSync(profilePath, JSON.stringify(profileWithoutIndex(profile), null, "\t") + "\n", "utf8");
    const headerPath = opts?.headerPath ||
        node_path_1.default.join(projectRoot, "include", "clpp", "generated", "datamodel.clh");
    node_fs_1.default.mkdirSync(node_path_1.default.dirname(headerPath), { recursive: true });
    node_fs_1.default.writeFileSync(headerPath, generateDatamodelHeader(profile), "utf8");
    return { profile, profilePath, headerPath };
}

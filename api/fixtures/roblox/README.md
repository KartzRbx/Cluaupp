# Roblox API fixtures

Use these dumps in CI without a local Studio install.

- `mini/` — tiny structural fixture for ingest tests
- Full dumps: point tests at `data/Mini-API-Dump.json` or set `CLUAUPP_API_DUMP`
